#!/bin/sh

name=<%= configuration["name"] %>
command=/etc/<%= configuration["name"] %>/app/bin/<%= Map.get(configuration, "binary", configuration["name"]) %>

source /etc/<%= configuration["name"] %>/bin/environment && ${command} <%= configuration["start"]["call"] %> > /var/log/<%= configuration["name"] %>.log 2>&1

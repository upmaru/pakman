#!/bin/sh

name=<%= configuration["name"] %>
command=/var/lib/<%= configuration["name"] %>/bin/<%= Map.get(configuration, "binary", configuration["name"]) %>

/var/lib/<%= configuration["name"] %>/bin/environment "${command} <%= configuration["start"]["call"] %>" > /var/log/<%= configuration["name"] %>.log 2>&1

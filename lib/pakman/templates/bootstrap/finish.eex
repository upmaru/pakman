#!/bin/sh

name=<%= configuration["name"] %>
command=/etc/<%= configuration["name"] %>/bin/<%= Map.get(configuration, "binary", configuration["name"]) %>

${command} <%= configuration["stop"]["call"] %>